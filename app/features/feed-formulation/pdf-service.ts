/**
 * Feed Formulation PDF Service
 * DISABLED - PDF export removed to reduce bundle size
 */

/**
 * Generate feed formulation PDF (disabled)
 */
export function generateFormulationPDF(_formulation: any): void {
  throw new Error('PDF export disabled to reduce bundle size')
}
